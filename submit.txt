const encrypt_pass = "...";
const encrypt_connection_string = "...";

const decodedString = Buffer.from(encrypt_pass, "base64").toString("utf8");
const Conn_String = Buffer.from(encrypt_connection_string, "base64").toString("utf8");

module.exports = {
    decodedString,
    Conn_String
};